# ============================================================================
# SCRIPT streaming SpeechLM — Granary 2.0, LoRA, no-blank, val_wer.
# WINDOWED RE-DECODING self-correction variant.
#
# Replaces the <del> backspace token with windowed re-decoding: each chunk is
# transcribed several times, each time with one more chunk of LOOKAHEAD audio,
# always conditioning on clean (believed-correct) history. Early emissions are
# low-latency provisional previews; later re-decodes (more lookahead) refine and
# finally LOCK each chunk. No delete token, no synthetic-error manufacturing --
# correction falls out of re-decoding with more acoustic context.
#
#   window size N = audio_history_chunks + 1  (chunks of audio per branch)
#   depth       R = redecode_depth            (max lookahead / lock lag, 1..M)
#
# Training adds, per chunk c, one branch per lookahead level j=0..R (j=0 is the
# base branch; higher j slides the audio window j chunks into the future).
# Inference emits each chunk immediately (j=0 preview) and locks it R chunks
# later at maximal lookahead. See
# nemo/collections/speechlm2/docs/script_windowed_redecoding.md
#
# INITIALIZE from a trained SCRIPT checkpoint (set INIT_CKPT in the launcher);
# j=0 matches the base audio_history_chunks=M objective, so it warm-starts well.
# ============================================================================

model:
  pretrained_llm: /lustre/fsw/portfolios/llmservice/users/heh/pretrained_models/huggingface/Qwen/Qwen3-1.7B
  pretrained_asr: /lustre/fsw/portfolios/llmservice/users/heh/pretrained_models/huggingface/nvidia/nemotron-speech-streaming-en-0.6b/nemotron-speech-streaming-en-0.6b.nemo
  load_llm_weights: true
  load_asr_weights: true

  sample_rate: 16000
  blank_token: ""            # no-blank: silent chunks emit <|im_end|>
  frame_length_in_secs: 0.08
  chunk_size: [2, 4, 7, 10, 14, 28]   # multi chunk-size: one drawn per batch
  att_context_size: [70, 1]  # left 70; right (lookahead) is set per batch to chunk-1
  audio_pad_to: 0

  # --- Windowed re-decoding ---
  redecode: true
  audio_history_chunks: 2    # window N = 3 chunks (M=2); each branch sees up to 2 prior chunks
  redecode_depth: 2          # R=2: re-decode up to 2 chunks back; final transcript lags 2 chunks

  compact_template: true

  freeze_speech_encoder: false
  freeze_modality_adapter: false
  freeze_modality_proj: false
  freeze_llm_model: true
  freeze_llm_head: false
  freeze_embed_tokens: false

  freeze_params: []
  prevent_freeze_params: []

  lora:
    task_type: CAUSAL_LM
    r: 128
    lora_alpha: 256
    lora_dropout: 0.01

  perception:
    modality_adapter:
      _target_: nemo.collections.speechlm2.modules.perception.IdentityConnector
    encoder:
      sync_max_audio_length: false

  optimizer:
    _target_: torch.optim.AdamW
    lr: 1e-4
    betas: [0.9, 0.98]
    weight_decay: 1e-3
    foreach: true

  lr_scheduler:
    _target_: nemo.core.optim.lr_scheduler.CosineAnnealing
    warmup_steps: 10000
    min_lr: 1e-6
    max_steps: ${trainer.max_steps}

data:
  # --- ScriptSTTDataset config ---
  dataset:
    sample_rate: ${model.sample_rate}
    frame_length_in_secs: ${model.frame_length_in_secs}
    chunk_size: ${model.chunk_size}
    num_delay_frames: -1  # -1 activates prompt-controlled delay (exact mode below)
    audio_tag: "<audio>"
    blank_token: ${model.blank_token}
    compact_template: ${model.compact_template}
    system_role: system

    # --- Exact-delay prompting ---
    exact_delay: true
    exact_max_delay: 4

    # --- Text-representation variation ---
    vary_text_repr: true
    text_repr_keep_chars: "'"

    # --- Windowed re-decoding (must match model.*) ---
    redecode: true
    audio_history_chunks: ${model.audio_history_chunks}
    redecode_depth: ${model.redecode_depth}
    redecode_train_prob: 1.0   # include every lookahead branch; lower this to cut training cost

    # Validation prompt (model.val_system_prompt reads data.dataset.system_prompt).
    system_prompt: "You are doing streaming speech recognition. Given the transcript so far and the next audio chunk, output the words spoken in that chunk. Emit each chunk's words with a fixed delay of 2 frames. Write the text with normal capitalization and punctuation."

  # Decode-only val_wer runs generate() on raw audio; pin a single fixed size.
  val_dataset_overrides:
    chunk_size: 14

  # --- Lhotse dataloader config ---
  train_ds:
    sample_rate: ${data.dataset.sample_rate}
    input_cfg: /lustre/fsw/portfolios/llmservice/projects/llmservice_nemo_speechlm/users/dongjig/aligned_amos/granary_v2_en_pnc_qwen_aligned_filtered/granary_v2_en_pnc_qwen_aligned_filtered_safe_iad_s3_audio.yaml
    skip_missing_manifest_entries: true
    manifest_filepath: null
    seed: 42
    shuffle: true
    shard_seed: "randomized"
    num_workers: 8
    force_iterable_dataset: true
    use_bucketing: true
    bucket_duration_bins: [4.32,6.0,7.04,7.92,8.8,9.6,10.4,11.12,11.89,12.66,13.47,14.8,16.92,20.0,22.4,29.44,35.0,39.6]
    bucket_batch_size: [38,29,25,22,20,18,17,15,14,13,12,11,10,8,7,6,5,4]
    max_duration: 54
    pad_extra_duration: 0.5
    pad_extra_duration_prob: 1.0

  validation_ds:
    datasets:
      mcv_11_dev:
        manifest_filepath: /lustre/fsw/portfolios/llmservice/projects/llmservice_nemo_speechlm/users/dongjig/aligned_amos/steve_val_mmlpc_mcv11_2k/mcv11_dev_clean_pcstrip_en_2k_qwen_aligned.json
    sample_rate: ${data.dataset.sample_rate}
    batch_size: 4
    num_workers: 4
    seed: 42
    shard_seed: "randomized"
    shuffle: false
    pad_extra_duration: 0.5
    pad_extra_duration_prob: 1.0

trainer:
  devices: -1
  accelerator: gpu
  num_nodes: 1
  precision: bf16-true
  logger: false
  enable_checkpointing: false
  use_distributed_sampler: false
  max_steps: 200000
  limit_train_batches: 2000
  val_check_interval: ${trainer.limit_train_batches}
  log_every_n_steps: 10
  num_sanity_val_steps: 1
  gradient_clip_val: 1.0
  accumulate_grad_batches: 1
  strategy:
    _target_: lightning.pytorch.strategies.DDPStrategy
    gradient_as_bucket_view: true
    find_unused_parameters: true


exp_manager:
  exp_dir: null
  explicit_log_dir: null
  name: streaming_stt_granary2_script_redecode
  create_tensorboard_logger: true
  create_checkpoint_callback: true
  use_datetime_version: true
  max_time_per_run: "00:03:50:00"

  resume_from_checkpoint: null
  resume_if_exists: true
  resume_ignore_no_checkpoint: true

  create_wandb_logger: false
  wandb_logger_kwargs:
    name: ${exp_manager.name}
    project: null
    resume: true

  checkpoint_callback_params:
    filename: "{step}-{val_wer:.4f}"
    monitor: val_wer
    mode: min
    every_n_train_steps: null
    every_n_epochs: 1
    save_top_k: 5
    always_save_nemo: false
    save_nemo_on_train_end: false
